// Deployment configuration - edit per environment.
window.ET_CONFIG = {
  // game server the "Deploy" button connects to (host:port as the engine
  // sees it; actual routing happens in the ws<->udp proxy)
  server: '127.0.0.1:27960',
  // total first-download size shown on the landing page
  downloadSize: '~230 MB',
};
